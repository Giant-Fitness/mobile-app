// utils/spacing.ts

import { moderateScale } from '@/utils/scaling';

export const spacing = {
    xxs: moderateScale(2), // Extra Extra small spacing
    xs: moderateScale(4), // Extra small spacing
    sm: moderateScale(8), // Small spacing
    md: moderateScale(16), // Medium spacing
    lg: moderateScale(24), // Large spacing
    xl: moderateScale(32), // Extra large spacing
    xxl: moderateScale(48), // 2xlarge spacing
    xxxl: moderateScale(64), // 3xlarge spacing
};
