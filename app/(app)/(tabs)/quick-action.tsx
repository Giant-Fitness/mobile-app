import React from 'react';
import { View } from 'react-native';

// This is a placeholder screen that should never be shown
// The tab press is intercepted in _layout.tsx
export default function QuickActionScreen() {
    return <View />;
}
