// constants/Spaces.ts

import { moderateScale } from '@/utils/scaling';

export const Spaces = {
    XXS: moderateScale(2), // Extra Extra small Spaces
    XS: moderateScale(4), // Extra small Spaces
    SM: moderateScale(8), // Small Spaces
    MD: moderateScale(16), // Medium Spaces
    LG: moderateScale(24), // Large Spaces
    XL: moderateScale(32), // Extra large Spaces
    XXL: moderateScale(48), // 2xlarge Spaces
    XXXL: moderateScale(64), // 3xlarge Spaces
};
